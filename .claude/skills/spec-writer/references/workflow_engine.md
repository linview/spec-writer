# 对话流程示例（workflow_engine.md）

> 本文件包含 spec-writer 完整对话流程的示例，供按需加载参考。

---

## Step 0: 模板扫描与选择

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 欢迎使用 Spec Writer
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Phase 1: 准备阶段

Step 1/4: 选择模板文件

可用模板（从 templates/ 扫描）：
  1. all_in_one.template.md        # All-in-One 三段论文档
  2. brd_prd.template.md           # BRD + PRD 两段论
  3. hld_backend.template.md       # 后端架构设计（分片）

选项：
  □ 从列表选择（输入序号 1-3）
  □ 使用默认模板

User: [选择 3]

✓ 已加载模板：hld_backend.template.md (v1.0.0)
  输出模式：sharded（10 个分片文档）
```

---

## Step 1: 提供前置知识

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Step 2/4: 提供前置知识（可选）⚠️ 强烈推荐
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

前置知识来源：
  □ 本地文档路径（如父文档、依赖文档）
  □ 在线文档 URL
  □ 网页 URL（如 GitHub README）
  □ 跳过

检测到可能的父文档：
  depot/devops/ci-health-tracker/ci-health-tracker_feature-cli_v1.0.0.md

User: [选择] 读取父文档

✓ 已读取父文档：ci-health-tracker_feature-cli_v1.0.0.md
  - 技术栈：Python 3.8+, Click, SQLite, GitLab API
  - 业务领域：CICD
  [已加载到上下文，用于智能推断]
```

---

## Step 2: 计划概要

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Step 4/4: 计划概要
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

将通过以下步骤创建文档：

Phase 1: BRD (4 个问题)
  1. BRD.background (需求背景)
  2. BRD.customer (客户是谁)
  3. BRD.benefits (预期收益)
  4. BRD.roi (是否值得做)

Phase 2: PRD (4 个问题)
  5. PRD.scope (功能范围)
  6. PRD.scenario (用户场景)
  7. PRD.features (功能需求)
  8. PRD.non_functional (非功能需求)

总计：16 个问题 | 预计耗时：15-20 分钟

能否开始？
  ☑ 是，开始创建
```

---

## Step 4: 对话创建

```
┌──────────────────────────────────────────┐
│ BRD.background (1/16), next: BRD.customer│
└──────────────────────────────────────────┘

当前问题：BRD.background (需求背景)

业务痛点是什么？为什么做这个功能？

[智能推断推荐 📘 来源于父文档]
  - 现有 CLI 工具不够直观，需要可视化界面
  - 缺乏实时监控，排障效率低

选项：
  □ 接受推荐
  □ 手动输入

User: [输入] 现有 CLI 工具需要查看大量日志，排障效率低

✓ BRD.background 已完成

┌──────────────────────────────────────────┐
│ BRD.customer (2/16), next: BRD.benefits  │
└──────────────────────────────────────────┘

当前问题：BRD.customer (客户是谁)

[智能推断推荐 📘 来源于父文档]
  - 目标客户：DevOps 团队
  - 开发人力：2 人

User: [接受]

✓ BRD.customer 已完成
```

---

## 分片模板对话示例

对于 `output_mode: "sharded"` 的模板（如 hld_backend），对话流程有所不同：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
分片 1/10：glossary
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[从 PRD 父文档提取术语...]

✓ 术语表已生成（18 个 PRD 术语 + 4 个预推断术语）

⚠️ 以下术语为自动推断，待审阅：
  - 聚合统计 [自动推断，待审阅]
  - 时间范围筛选 [自动推断，待审阅]
  - 项目筛选 [自动推断，待审阅]
  - 定时同步 [自动推断，待审阅]

User: [确认]

✓ glossary 分片完成 (v0.1.0)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
分片 2/10：system_architecture
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[遇到 spec-writer 指令：架构选型推荐]

📊 主推荐方案：

| 维度 | 推荐方案 | 推荐依据 |
|------|---------|---------|
| 框架 | FastAPI 0.100+ | PRD 要求 RESTful API |
| 数据库 | PostgreSQL 12+ | PRD 技术约束 |
| ... | ... | ... |

置信度：高

User: [接受]
```

---

## 存量文档检查

```
🔍 检查存量文档

现有文档：docs/old-spec.md

请选择用于对比的模板：
  1. all_in_one.template.md
  2. 输入自定义模板路径

User: [选择 1]

✓ 使用模板：all_in_one.template.md
  正在对比文档结构...

发现 3 个缺失章节：
  ❌ PRD → UI/UX 需求
  ⚠️ 字段未填：BRD → ROI 分析
  ⚠️ 格式问题：YAML metadata 缺少 spec_type

是否补充缺失内容？
  ☑ 是
```

---

## 草稿恢复

```
📂 恢复草稿

草稿 ID：draft-20260227-103000
创建时间：2026-02-27 10:30
最后修改：2026-02-27 11:15

进度概要：
  ✓ BRD.background（已完成）
  ✓ BRD.customer（已完成）
  ⏳ BRD.benefits（进行中）
  ⏸️ BRD.roi（待处理）
  ...

当前进度：3/16 (19%), next: BRD.benefits

是否从此处继续？
  ☑ 是，继续填写
  □ 修改已完成内容
```
